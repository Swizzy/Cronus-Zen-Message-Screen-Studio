assembly-versioning-scheme: MajorMinorPatch
mode: Mainline
next-version: 1.2.0
commit-message-incrementing: Enabled
branches:
  dev:
    regex: dev(elop)?(ment)?$
    tag: beta
    source-branches: ['master']
